describe "ProblemsController" do
  describe "#audit" do
    it "should run the query as the subclassed type" do
    end

    it "should delete the resource if the query returns false" do
    end

    it "should not delete the resource if the query returns true" do
    end
  end
end
