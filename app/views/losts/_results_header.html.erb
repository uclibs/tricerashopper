<thead>
  <tr>
    <th> <%= Lost.human_attribute_name(:item_number) %> </th>
    <th> <%= link_to "Title", request.params.merge({sort: :title}) %> </th>
    <th> <%= Lost.human_attribute_name(:status) %> </th>
    <th> <%= Lost.human_attribute_name(:location) %> </th>
    <th> <%= link_to "Call Number", request.params.merge({sort: :call_number}) %> </th>
    <th> <%= Lost.human_attribute_name(:barcode) %> </th>
    <th> <%= Lost.human_attribute_name(:last_checkout) %> </th>
  </tr>
</thead>
